import { ViewStatusPipe } from './view-status.pipe';

describe('ViewStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
